export interface INavItems {
  title: string,
  link: string,
  commponent: JSX.Element,
};

export interface User {
    brukerID: number,
    name: string,
    username: string,
    email: string,
    home: string
}
